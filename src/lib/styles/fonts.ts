import { Inter } from "next/font/google";
import { Playwrite_AU_QLD } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
});

export const playwrite_AU_QLD = Playwrite_AU_QLD({
  weight: "400",
});
